export interface IInventoryItem {
  title: string;
  description: string;
  quantity: number;
  price: number;
  totalPrice: number;
  category: string;
  image?: string;
  SOID: string;
  id: string;
}
