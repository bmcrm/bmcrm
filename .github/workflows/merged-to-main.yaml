name: MERGED TO MAIN

on:
  push:
    branches:
    - main

jobs:
  deploy-to-dev:
    uses: dusty-lab/bmcrm/.github/workflows/deploy.yaml@main
    with:
      env: dev
    secrets:
      AWS_ACCESS_KEY_ID: ${{ secrets.DEV_AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.DEV_AWS_SECRET_ACCESS_KEY }}

  smoke-tests-dev:
    needs: [deploy-to-dev]
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - run: npx playwright test src/tests

  # deploy-to-prod:
  #   needs: [smoke-tests-dev]
  #   uses: dusty-lab/bmcrm/.github/workflows/deploy.yaml@main
  #   with:
  #     env: prod
  #   secrets:
  #     AWS_ACCESS_KEY_ID: ${{ secrets.PROD_AWS_ACCESS_KEY_ID }}
  #     AWS_SECRET_ACCESS_KEY: ${{ secrets.PROD_AWS_SECRET_ACCESS_KEY }}
  #     NPM_PUBLISHER_PAT: ${{ secrets.NPM_PUBLISHER_PAT }}